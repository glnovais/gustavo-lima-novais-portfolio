import { ArrowUpRight, Command, Github, Linkedin, Mail } from 'lucide-react';
import { profile } from '../../config/profile';
import { useI18n } from '../../i18n/I18nProvider';

export default function Contact({ onOpenPalette }: { onOpenPalette?: () => void }) {
  const { t } = useI18n();
  return <section id="contato" className="section"><div className="container-shell"><div className="panel contact-panel p-6 md:p-10 relative overflow-hidden"><div className="absolute -right-24 -top-24 w-72 h-72 rounded-full bg-cyan-300/[.05] blur-3xl"/><div className="relative grid xl:grid-cols-[1fr_auto] gap-8 items-end"><div><div className="section-kicker">{t('contact.kicker')}</div><h2 className="text-[clamp(2.5rem,6vw,5.4rem)] leading-[.95] tracking-[-.055em] font-semibold max-w-3xl">{t('contact.title')}</h2><p className="section-copy mt-6">{t('contact.copy')}</p><div className="mt-5 text-sm text-slate-500 mono">{profile.email}</div></div><div className="flex flex-col gap-2 min-w-[260px]"><a className="btn btn-primary justify-between" href={`mailto:${profile.email}`}><span className="flex items-center gap-2"><Mail size={16}/>{t('contact.email')}</span><ArrowUpRight size={16}/></a><a className="btn btn-ghost justify-between" href={profile.linkedin} target="_blank" rel="noreferrer"><span className="flex items-center gap-2"><Linkedin size={16}/>LinkedIn</span><ArrowUpRight size={16}/></a>{profile.github ? <a className="btn btn-ghost justify-between" href={profile.github} target="_blank" rel="noreferrer"><span className="flex items-center gap-2"><Github size={16}/>GitHub</span><ArrowUpRight size={16}/></a> : <div className="btn btn-disabled justify-between" aria-disabled="true"><span className="flex items-center gap-2"><Github size={16}/>GitHub</span><span className="text-xs">{t('contact.githubSoon')}</span></div>}<button onClick={onOpenPalette} className="btn btn-ghost justify-between"><span className="flex items-center gap-2"><Command size={16}/>{t('contact.palette')}</span><span className="mono text-xs">Ctrl K</span></button></div></div></div></div></section>;
}
